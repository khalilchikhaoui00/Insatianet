<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { text-align: center; margin-bottom: 30px; }
        .order-details { background: #f9f9f9; padding: 20px; border-radius: 5px; }
        .thank-you { text-align: center; margin-top: 30px; }
        .button { display: inline-block; padding: 10px 20px; background: #4CAF50; color: white; text-decoration: none; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Confirmation de votre commande #{{ order.id }}</h1>
        </div>

        <p>Bonjour {{ order.user.firstName }},</p>

        <p>Nous vous confirmons que votre commande a bien été enregistrée. Voici les détails :</p>

        <div class="order-details">
            <p><strong>Numéro de commande :</strong> #{{ order.id }}</p>
            <p><strong>Date :</strong> {{ order.orderDate|date('d/m/Y H:i') }}</p>
            <p><strong>Montant total :</strong> {{ order.totalAmount }}€</p>
            <p><strong>Statut :</strong> {{ order.status }}</p>
            <p><strong>Adresse de livraison :</strong><br>{{ order.shippingAddress|nl2br }}</p>
        </div>

        <div class="thank-you">
            <p>Merci pour votre confiance !</p>
            <p>Une facture détaillée est jointe à cet email.</p>
            <a href="{{ url('app_order_show', {'id': order.id}) }}" class="button">Voir ma commande</a>
        </div>
    </div>
</body>
</html> 